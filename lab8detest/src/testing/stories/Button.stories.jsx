import { Button } from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    backgroundColor: "#1a1a1a",
    label: "2",
    size: "large",
    primary: true
  }
};